package com.duyp.architecture.clean.android.powergit.ui.base

/**
 * Interface represents a view state object which a ViewModel uses to populate state to a fragment / activity (view)
 */
interface ViewState